# VARIABLES is something that stores the value for me locally

name = input("Enter Your Name: ") # name is variable here
print(name) # we are printing the variable

# Task : Get the user input and find the length of the length of the user input and print it in the console/terminal
print(len(input("User Input: ")))

# Task : Get the user input and find the length of the length of the user input and print it in the console/terminal using variable
user_input = input("User Input: " )
length = len(user_input)
print(length)